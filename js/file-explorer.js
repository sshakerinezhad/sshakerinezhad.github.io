/**
 * FileExplorer - Windows-style file browser
 */
const FileExplorer = {
  init(container, config) {
    console.log('FileExplorer.init()', { container, config });
    // Step 2 will add: fetch data, render, navigation
  }
};
